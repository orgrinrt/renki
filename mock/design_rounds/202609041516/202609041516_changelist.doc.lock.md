# doc changelist

Nothing in the templates changes. The readme paragraph naming `Tool::commands`
landed in round `202609041506`; what this round does is let the readme truth
test admit the name, which is a test under `tests/` and so needs the source
window. Descends from the topic file
`202609041516_topic.the-readme-list-lags-the-command-table.md`.

## Claims the source changelist will carry

The row, the compiler-checked reference and the count in
`the_readme_tells_the_truth_about_the_package.rs`.
