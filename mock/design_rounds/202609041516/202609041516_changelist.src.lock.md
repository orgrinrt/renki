# src changelist

The row, the reference and the count. Descends from the locked doc changelist
`202609041516_changelist.doc.lock.md`.

## CHANGE: `NAMEABLE` in the readme truth test FROM `34 rows, no `Tool::commands`` TO `35 rows with `Tool::commands`, referenced through `Tool::CONVENTIONS.commands``
File: `mock/crates/renki/tests/the_readme_tells_the_truth_about_the_package.rs`
Verification: `grep -c '"Tool::commands",' mock/crates/renki/tests/the_readme_tells_the_truth_about_the_package.rs` is 1; `grep -n 'Tool::CONVENTIONS.commands' mock/crates/renki/tests/the_readme_tells_the_truth_about_the_package.rs` returns one match inside `every_name_here_is_a_name_this_crate_has`; `cargo test -p renki --test the_readme_tells_the_truth_about_the_package` is green over eleven tests, where it was red on `the_readme_names_nothing_this_crate_does_not_have` before.

## Not changed, and why

The verifications of the round before this one ran `cargo test -p renki`
without `--test`, which is what let the integration test stay unrun until the
push gate. The closed round's changelist is an archive and stays as it was;
the lesson is in the topic file.
