# doc changelist

Three sentences in two design templates, and the documents rendered from
them. Descends from the topic file
`202609041906_topic.the-rendered-documents-name-no-round.md`.

## What lands

- `mock/crates/renki/DESIGN.md.tmpl`: the opening says what the
  configuration adds, and the section is "Configuration".
- `mock/crates/renki-dirs/DESIGN.md.tmpl`: the table keeps the borrowed form
  for now, with no round named.
- `docs/RENKI.md` and `docs/DIRS.md`, rendered.

## Claims the source changelist will carry

None. The source changelist exists so the round closes, and says so.
