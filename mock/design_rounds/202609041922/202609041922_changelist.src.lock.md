# src changelist

Nothing in the source changes. The round is two install lines and the render
in the locked doc changelist `202609041922_changelist.doc.lock.md`, and this
file exists so the round closes. The three manifests move to `0.0.3`, `0.0.1`
and `0.0.1` in the same pull request, outside the gate, with the root readme's
line and the changelog.

## Not changed, and why

Every source file, since a release moves versions and not code.
