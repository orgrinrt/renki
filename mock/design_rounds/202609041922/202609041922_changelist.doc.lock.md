# doc changelist

One line in each of two readmes, and the document rendered from them.
Descends from the topic file
`202609041920_topic.the-install-lines-name-the-released-versions.md`.

## What lands

- `mock/crates/renki-dirs/README.md`: the install line names `0.0.1`.
- `mock/crates/renki-config/README.md`: the install line names `0.0.1`.
- `docs/STRUCTURE.md`, rendered.

## Claims the source changelist will carry

None. The manifests move to the versions the lines name, outside the gate,
and the source changelist exists so the round closes, and says so.
