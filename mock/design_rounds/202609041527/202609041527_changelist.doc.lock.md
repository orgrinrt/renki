# doc changelist

The launcher's design document, amended to say what the review's fixes make
true, and to carry the choice the round before this one left unrecorded. It
descends from the topic file
`202609041527_topic.the-review-findings-on-the-command-table.md`.

## What lands

- `mock/crates/renki/DESIGN.md.tmpl`, section "A tool's own commands": an
  `Invocation` is read through accessors and built by nobody outside the
  crate; its arguments are what followed the name with the launcher's flags
  taken out wherever they sat; the descriptor check also refuses a name that
  is the directory flag, the engine flag, or `--cfg` on a tool with settings;
  and the alternative chosen against, a match in the consumer's `main`, with
  the two reasons the table wins.
- `README.md` at the repo root, hand-written: the sentence on what the
  function gets says the flags come out wherever they sat.

## Claims the source changelist will carry

`Invocation` with private fields, accessors and a crate-private constructor;
the three flag arms in `commands_defect` with a control each; the `locate`
arm in the dispatch-order test; a const assertion per refusal arm; the doc on
the arguments; the readme; and the rendered documents.
