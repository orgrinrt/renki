# src changelist

Carries out `202609181119_changelist.doc.lock.md`. The one open topic,
`202609181119_topic.ls-remote-answers-within-a-deadline.md`, is this round's
and is what everything below does.

## CHANGE: module `ask` FROM absent TO a deadline runner and the quiet `git`
File: `mock/crates/renki/src/ask.rs`, declared in `mock/crates/renki/src/lib.rs`
Verification: `grep -n "^mod ask;" mock/crates/renki/src/lib.rs` returns one match.

## CHANGE: fn `ask::run_within` FROM absent TO `(Command, Duration, &str) -> Result<Output, String>`
File: `mock/crates/renki/src/ask.rs`
Verification: stdin null, stdout and stderr into two `Scratch` files, the child
killed and an `Err` naming the deadline once it passes. `cargo test -p renki ask`
runs the tests for each: past the deadline, both streams and the status,
more than a pipe holds, stdin at end of file, output held open by a background
process answered at the child's exit, a child that cannot start.
`grep -c "thread::spawn\|dyn " mock/crates/renki/src/ask.rs` returns 0.

## CHANGE: struct `ask::Scratch` FROM absent TO a scratch file removed on drop
File: `mock/crates/renki/src/ask.rs`
Verification: `create_new` in the temporary directory, retried on a taken
name; `Drop` removes the file. Tests: gone once dropped and two never share a
name; reads back from its start. Removing the `remove_file` or seeking from
the current position instead of the start fails them.

## CHANGE: trait `ask::SshSources` and struct `ask::ThisProcess` FROM absent TO git's three ssh settings
File: `mock/crates/renki/src/ask.rs`
Verification: `grep -n "fn core_ssh_command(&self, deadline: Duration) -> Option<OsString>" mock/crates/renki/src/ask.rs`
returns one match; `ThisProcess` reads the two environment variables and
`core.sshCommand` through `configured_value(core_ssh_command_query(), deadline)`.

## CHANGE: fn `ask::quiet_git` FROM absent TO `(&impl SshSources, Duration) -> Command`
File: `mock/crates/renki/src/ask.rs`
Verification: `GIT_TERMINAL_PROMPT=0` always; `GIT_SSH_COMMAND="ssh -o BatchMode=yes"`
only where all three sources are silent, read in rank order and stopping at the
first that answers. Tests over all eight combinations, and on how often the
configuration is asked.

## CHANGE: fns `ask::core_ssh_command_query` and `ask::configured_value` FROM absent TO reading `core.sshCommand` within a deadline
File: `mock/crates/renki/src/ask.rs`
Verification: tests against a repository's own config with the global and
system config isolated, set, unset and empty, and against a query too slow or
failing.

## CHANGE: fn `pin::ls_remote_head` FROM `Command::output()` TO `ls_remote_head_from(&ThisProcess, url, branch, ASK_DEADLINE)`
File: `mock/crates/renki/src/pin.rs`
Verification: `grep -n "output()" mock/crates/renki/src/pin.rs` returns nothing.
`ls_remote_head_from` hands `run_within` `deadline.saturating_sub(started.elapsed())`;
the test whose configuration takes the whole deadline gets `did not answer
within 0 seconds`, and giving the listing the full deadline instead fails it.
