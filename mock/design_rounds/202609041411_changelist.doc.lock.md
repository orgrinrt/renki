# doc changelist

The design documents of the two crates the review findings touch, amended to
say what the fixes make true. They descend from the workspace design
`.shared/designs/renki-configuration.md`, amended beside this round so the
variable the engine gets is `<SHORT>_CONFIG_FILE` there too, and from the topic
file `202609041409_topic.the-review-findings-on-the-configuration-round.md`.

## What lands

- `mock/crates/renki/DESIGN.md.tmpl`: the engine's variable is
  `<SHORT>_CONFIG_FILE` and `<SHORT>_CONFIG` is the directory's override; a tool
  with no settings keeps `--cfg` for its engine; `set` and `edit` create the
  config directory, and where `set` puts a key the file does not have yet,
  including above the first header for a top-level key; the launcher's own
  reinstall goes through the same retry as the engine's build.
- `mock/crates/renki-config/DESIGN.md.tmpl`: the list kind's canonical text
  form, and that a text item holding a quote is refused because that form
  cannot carry one.

## Claims the source changelist will carry

The `LICENSE` beside the launcher's manifest, the directory `set` creates, the
top-level region in the writer, the reinstall retry, the guarded `--cfg`, the
two comments naming the right variables, the refused quote, the readme truth
test and doctest for `renki-config`, and the two catalogue-red tests for the
prerequisite checks the design names and the source does not do yet. Each is a
`CHANGE` block there with its own verification.
